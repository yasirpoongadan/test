import { MysearchFilterPipe } from './mysearch-filter.pipe';

describe('MysearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MysearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
